const PORT = 3001;
const SERVER_URL = `http://localhost:${PORT}`;

// Locales
const LOCALES = `${SERVER_URL}/api/locales`;

// Status
const STATUS = `${SERVER_URL}/api/status`;

export { LOCALES, STATUS };
